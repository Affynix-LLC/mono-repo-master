{
  "name": "@affynix/workers",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev src/index.ts",
    "build": "wrangler deploy --dry-run",
    "deploy": "wrangler deploy src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@affynix/shared": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240529.0",
    "wrangler": "^3.60.0",
    "typescript": "^5.4.5"
  }
}
